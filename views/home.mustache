<!DOCTYPE html>
<html>

  <head>
    <link rel="stylesheet" href="screen.css">
    <link href="https://fonts.googleapis.com/css?family=Special+Elite" rel="stylesheet">
    <meta charset="utf-8" lang="en">
    <title>/CIA/topSecret/</title>
  </head>

  <body>

    <div class="wrapper">
      <div class="header">
        <h3>System locks after {{guessesLeft}} inputs</h3>
        <h3>the alarms will sounds</h3>
      </div>
      <div class="messageWrapper">
        <div id="message">
          {{#message}}
            <span>{{.}}</span>
          {{/message}}
          {{^message}}
          <span> </span>
          {{/message}}
        </div>
      </div>
      <div class="flex">
        <div class="game">
          {{#mysteryWord}}
            <span>{{.}}</span>
          {{/mysteryWord}}
          <form action="/guess" method="post">
            <input autofocus="autofocus" type="text" name="letter" placeholder="Guess a Letter">
            <button type="submit" name="btn">enter</button>
          </form>
          <div class="guesses">
            <span>Letters Guessed:</span>
            {{#lettersGuessed}}
              <span>{{.}}</span>
            {{/lettersGuessed}}
          </div>
        </div>
      </div>
    </div>
  </body>

</html>
